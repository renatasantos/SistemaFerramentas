<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.renata.sistemaferramentas">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icone_construcao"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuPrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastrarFerramentaActivity" />
        <activity android:name=".AlterarDadosActivity" />
        <activity android:name=".ConsultarFerramentaActivity" />
        <activity android:name=".ExcluirFerramentaActivity" />
        <activity android:name=".BuscaFerramentaActivity"></activity>
    </application>

</manifest>